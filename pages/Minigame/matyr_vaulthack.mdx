# matyr_vaulthack
## esx-qb

exports['matyr_vaulthack']:vault(time, 4, step, function(success)
    if success then
        -- Successfully completed the Vault minigame
    else
        -- Failed to complete the Vault minigame
    end
end)
