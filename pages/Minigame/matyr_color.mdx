# matyr_color
## esx-qb

local success = exports['matyr_color']:color(amount_icon, time)
if success then
    -- Successfully completed the Color minigame
else
    -- Failed to complete the Color minigame
end
