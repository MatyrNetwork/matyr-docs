# matyr_password
## esx-qb

local result = exports['matyr_password']:password(length, time, showtime)
if result then
    -- Successfully completed the Password minigame
else
    -- Failed to complete the Password minigame
end
